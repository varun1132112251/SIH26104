# FastAPI base URL. The frontend defaults to http://localhost:8000.
VITE_API_BASE_URL=http://localhost:8000

# Leave true for local UI development until a backend detection endpoint exists.
VITE_USE_MOCK_API=true

# Set this only when the backend team publishes the real detection route.
# VITE_DETECTION_ENDPOINT=/api/v1/detections
